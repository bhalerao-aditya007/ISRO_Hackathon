/**
 * PRISM Frontend — Runtime Configuration
 * -----------------------------------------------------------------------
 * TODO(backend): Set this to your deployed API origin once the backend
 * is ready, e.g. "https://api.prism-lunar.example.com".
 * Leave empty ("") to keep the UI running on mock data only.
 *
 * If you're deploying both frontend and backend on Vercel, you can also
 * set this dynamically via an environment variable injected at build
 * time, or simply point it at a relative "/api" path if the backend is
 * served from the same Vercel project as serverless functions.
 */
window.PRISM_CONFIG = {
  API_BASE_URL: "https://Atharv1909-prism-api.hf.space", 
};
